git : 2/5/22

create account
in git, create new repo-
	description, click public, tick readme file -and then create 
	
under code ---select https for cloning 
open git bash ----
	cd desktop  /cd folder name
	git clone (paste the copied clone comment)
	
push code local to hub
create python file in  folder created in desktop ---inside folder eg:test.py

ls --->lists all the file
git status : branch name ,untracked files , test.py

git add  . --all file 
git add  filename ---------eg: git add test.py

after adding 
git commit -m "message here " --------------(m means message)\

after commit
git push branchname ---------eg : git push origin main

creatw branch 

git branch-------------list all branch 
git checkout branchname--------to switch between branches

git status -toc check changes

pull request and ,erge request

git clone 
git checkout <branch name>
git status
git add <file name>
git checkout -b <branch name>
git push origin <branchname>
commands to learn 
git rebase
git reset
git rebase -I
git merge

===================================================
1) open the folder location in git bash
2) git clone <paste the repo https>
3) git cd to folder 
4) git add 'filename.py'
5) git commit -m 'message'
6) git push origin <branch name>

===================================
for creating new branch
1) git branch --> to list all branches in repo
2) git branch branchName -----------> to create new branch
3) git branch ---> to check list of branch
4) git checkout branchName -----> to switch to given branchName
	---> and den push the code
